# Database
DATABASE_URL=postgresql://crewsplit:crewsplit@localhost:5432/crewsplit

# Session secret (generate with: openssl rand -hex 32)
SESSION_SECRET=change-me-to-a-random-64-char-hex-string

# App settings (optional — can also be set in admin panel)
# NEXT_PUBLIC_APP_NAME=CrewSplit
